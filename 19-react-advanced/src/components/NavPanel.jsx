import React from 'react'
import {useNavigate} from 'react-router-dom'
const NavPanel = () => {
  const navigate = useNavigate()
  
  

  return (
    <div>
        {/* navigating in page using useNavigate */}

    </div>
  )
}

export default NavPanel